# External Secrets Operator installation via Helm
# This is a reference - you can install via Helm chart or use the operator directly
# For production, consider using HelmRelease with Flux or Argo CD Application

apiVersion: v1
kind: Namespace
metadata:
  name: external-secrets
---
# Note: This is a simplified example. In practice, you would:
# 1. Install ESO via Helm chart: helm install external-secrets external-secrets/external-secrets -n external-secrets --create-namespace
# 2. Or create an Argo CD Application that points to the Helm chart
# 3. Configure IRSA (IAM Roles for Service Accounts) for the ESO service account

